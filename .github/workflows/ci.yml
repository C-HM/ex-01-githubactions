name: CI Workflow

# Trigger on push to main branch
on:
  push:
    branches: ["main"]

jobs:
  build-and-lint:
    runs-on: ubuntu-latest

    steps:
      # 1. Checkout code (Updated to v4)
      - uses: actions/checkout@v4

      # 2. Install NodeJS (Updated to v4)
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "18"

      # 3. Install dependencies
      - name: Install dependencies
        run: npm install

      # 4. Execute Lint
      - name: Run Lint
        run: npm run lint

      # 5. Create Artifact (Updated to v4)
      # v4 is faster and more secure, but requires unique names if run multiple times
      - name: Archive Production Artifact
        uses: actions/upload-artifact@v4
        with:
          name: node-project-artifact
          path: |
            .
            !node_modules
